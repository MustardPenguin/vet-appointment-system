
server:
  port: 8761

spring:
  application:
    name: "eureka-server"

logging:
  level:
    org:
      springframework: DEBUG

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  instance:
    prefer-ip-address: true
    hostname: localhost